filter {
  if [type] == "harbor" {
    mutate {
    add_field => { "[event][category]" => "harbor"}
    }

  grok {
   match => {
    "[event][original]" => [
      "<%{POSINT:syslog_pri}>%{POSINT} %{TIMESTAMP_ISO8601:syslog_timestamp} %{HOSTNAME:hostname} %{GREEDYDATA:data}"
    ]
   }
  }
 }
}

